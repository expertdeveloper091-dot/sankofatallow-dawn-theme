{%- comment -%}
  Custom CTA Section (Infinite Loop Vertical Sliding Image Tracks + 3-Column Grid)
  Left/Right side images are 100% dynamic via "Sliding Image Card" blocks (side: left/right).
  Slide columns are completely hidden on mobile (<=900px).
{%- endcomment -%}

{%- liquid
  assign photo_blocks = section.blocks | where: 'type', 'photo'
  assign slide_blocks = section.blocks | where: 'type', 'slide'
  assign all_photos = photo_blocks | concat: slide_blocks
  assign avatar_blocks = section.blocks | where: 'type', 'avatar'

  assign left_photos = all_photos | where: 'side', 'left'
  assign right_photos = all_photos | where: 'side', 'right'
-%}

<div class="maison-cta_section-{{ section.id }} maison-cta_section">

  {%- if section.settings.enable_marquee -%}
    <!-- LEFT sliding column (slides UP) -->
    <div class="slide-col left-col">
      <div class="slide-track up">
        {%- liquid
          assign left_count = left_photos.size
          assign repeat_times = 2
          if left_count > 0 and left_count < 3
            assign repeat_times = 6
          elsif left_count >= 3 and left_count < 5
            assign repeat_times = 4
          endif
        -%}
        {%- if left_count > 0 -%}
          {%- for i in (1..repeat_times) -%}
            {%- for block in left_photos -%}
              <div class="cta-photo-card" {{ block.shopify_attributes }}>
                {%- if block.settings.image != blank -%}
                  <img src="{{ block.settings.image | image_url: width: 400 }}" alt="{{ block.settings.title | default: 'Product photo' | escape }}" loading="lazy" width="200" height="200">
                {%- else -%}
                  {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                {%- endif -%}
                {%- if block.settings.title != blank -%}
                  <span>{{ block.settings.title | escape }}</span>
                {%- endif -%}
              </div>
            {%- endfor -%}
          {%- endfor -%}
        {%- elsif all_photos.size > 0 -%}
          {%- for i in (1..3) -%}
            {%- for block in all_photos -%}
              <div class="cta-photo-card" {{ block.shopify_attributes }}>
                {%- if block.settings.image != blank -%}
                  <img src="{{ block.settings.image | image_url: width: 400 }}" alt="{{ block.settings.title | default: 'Product photo' | escape }}" loading="lazy" width="200" height="200">
                {%- else -%}
                  {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                {%- endif -%}
                {%- if block.settings.title != blank -%}
                  <span>{{ block.settings.title | escape }}</span>
                {%- endif -%}
              </div>
            {%- endfor -%}
          {%- endfor -%}
        {%- else -%}
          {%- for i in (1..4) -%}
            <div class="cta-photo-card"><img src="https://images.unsplash.com/photo-1544005313-94ddf0286df2?w=400&auto=format&fit=crop&q=80" alt="Whipped Tallow Balm" loading="lazy" width="200" height="200"><span>Whipped Tallow Balm</span></div>
            <div class="cta-photo-card"><img src="https://images.unsplash.com/photo-1534528741775-53994a69daeb?w=400&auto=format&fit=crop&q=80" alt="Herbal Face Cream" loading="lazy" width="200" height="200"><span>Herbal Face Cream</span></div>
            <div class="cta-photo-card"><img src="https://images.unsplash.com/photo-1517841905240-472988babdf9?w=400&auto=format&fit=crop&q=80" alt="Honey Glow Serum" loading="lazy" width="200" height="200"><span>Honey Glow Serum</span></div>
          {%- endfor -%}
        {%- endif -%}
      </div>
    </div>
  {%- endif -%}

  <!-- CENTER Content -->
  <div class="maison-cta_content">

    {%- if section.settings.show_badge -%}
      <div class="maison-cta_badge">
        <div class="maison-cta_avatars">
          {%- for block in avatar_blocks -%}
            {%- if block.settings.image != blank -%}
              <img src="{{ block.settings.image | image_url: width: 80 }}" alt="Customer" class="maison-cta_avatar" width="40" height="40" {{ block.shopify_attributes }}>
            {%- else -%}
              <div class="maison-cta_avatar maison-cta_avatar-placeholder" {{ block.shopify_attributes }}>👤</div>
            {%- endif -%}
          {%- endfor -%}
        </div>
        <span class="maison-cta_badge-text">
          <b>{{ section.settings.badge_count | default: '5,000+' }}</b>
          <em>{{ section.settings.badge_text | default: 'Happy Customers' }}</em>
        </span>
      </div>
    {%- endif -%}

    <h1 class="maison-cta_heading">
      <span class="maison-cta_highlight">{{ section.settings.heading_highlight | default: 'DISCOVER YOUR' }}</span>
      <span class="accent">{{ section.settings.heading_rest | default: 'HEALTHIEST SKIN NATURALLY' }}</span>
    </h1>

    {%- if section.settings.description != blank -%}
      <p class="maison-cta_description">{{ section.settings.description }}</p>
    {%- endif -%}

    {%- if section.settings.button_text != blank -%}
      <a href="{{ section.settings.button_link | default: '#' }}" class="maison-cta_button">
        {{ section.settings.button_text }}
      </a>
    {%- endif -%}

    <div class="maison-cta_benefits">
      {%- for i in (1..3) -%}
        {%- assign benefit_key = 'benefit_' | append: i -%}
        {%- assign benefit_text = section.settings[benefit_key] -%}
        {%- if benefit_text != blank -%}
          <div class="maison-cta_benefit">
            <span class="tick">✓</span>
            <span class="maison-cta_text">{{ benefit_text }}</span>
          </div>
        {%- endif -%}
      {%- endfor -%}
    </div>
  </div>

  {%- if section.settings.enable_marquee -%}
    <!-- RIGHT sliding column (slides DOWN) -->
    <div class="slide-col right-col">
      <div class="slide-track down">
        {%- liquid
          assign right_count = right_photos.size
          assign repeat_times = 2
          if right_count > 0 and right_count < 3
            assign repeat_times = 6
          elsif right_count >= 3 and right_count < 5
            assign repeat_times = 4
          endif
        -%}
        {%- if right_count > 0 -%}
          {%- for i in (1..repeat_times) -%}
            {%- for block in right_photos -%}
              <div class="cta-photo-card" {{ block.shopify_attributes }}>
                {%- if block.settings.image != blank -%}
                  <img src="{{ block.settings.image | image_url: width: 400 }}" alt="{{ block.settings.title | default: 'Product photo' | escape }}" loading="lazy" width="200" height="200">
                {%- else -%}
                  {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                {%- endif -%}
                {%- if block.settings.title != blank -%}
                  <span>{{ block.settings.title | escape }}</span>
                {%- endif -%}
              </div>
            {%- endfor -%}
          {%- endfor -%}
        {%- elsif all_photos.size > 0 -%}
          {%- for i in (1..3) -%}
            {%- for block in all_photos -%}
              <div class="cta-photo-card" {{ block.shopify_attributes }}>
                {%- if block.settings.image != blank -%}
                  <img src="{{ block.settings.image | image_url: width: 400 }}" alt="{{ block.settings.title | default: 'Product photo' | escape }}" loading="lazy" width="200" height="200">
                {%- else -%}
                  {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                {%- endif -%}
                {%- if block.settings.title != blank -%}
                  <span>{{ block.settings.title | escape }}</span>
                {%- endif -%}
              </div>
            {%- endfor -%}
          {%- endfor -%}
        {%- else -%}
          {%- for i in (1..4) -%}
            <div class="cta-photo-card"><img src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=400&auto=format&fit=crop&q=80" alt="Coconut Body Butter" loading="lazy" width="200" height="200"><span>Coconut Body Butter</span></div>
            <div class="cta-photo-card"><img src="https://images.unsplash.com/photo-1524504388940-b1c1722653e1?w=400&auto=format&fit=crop&q=80" alt="Grass-Fed Tallow" loading="lazy" width="200" height="200"><span>Grass-Fed Tallow</span></div>
            <div class="cta-photo-card"><img src="https://images.unsplash.com/photo-1539571696357-5a69c17a67c6?w=400&auto=format&fit=crop&q=80" alt="Chamomile Soothe" loading="lazy" width="200" height="200"><span>Chamomile Soothe</span></div>
          {%- endfor -%}
        {%- endif -%}
      </div>
    </div>
  {%- endif -%}

</div>

{% style %}
  .maison-cta_section-{{ section.id }} {
    --bg: {{ section.settings.background_color | default: '#C1663D' }};
    --border-color: {{ section.settings.border_color | default: 'rgba(255,255,255,0.2)' }};
    --heading-color: {{ section.settings.heading_color | default: '#2B1B12' }};
    --highlight-color: {{ section.settings.highlight_color | default: '#2B1B12' }};
    --accent-color: {{ section.settings.accent_text_color | default: '#FDF3E7' }};
    --desc-color: {{ section.settings.description_color | default: '#3d2a1c' }};
    --badge-bg: {{ section.settings.badge_bg | default: '#ffffff' }};
    --badge-text: {{ section.settings.badge_text_color | default: '#2B1B12' }};
    --btn-bg: {{ section.settings.button_bg | default: '#2B1B12' }};
    --btn-text: {{ section.settings.button_text_color | default: '#ffffff' }};
    --btn-border: {{ section.settings.button_border | default: '#2B1B12' }};
    --btn-shadow: {{ section.settings.button_shadow_color | default: 'rgba(0,0,0,0.25)' }};
    --icon-bg: {{ section.settings.icon_bg | default: '#2B1B12' }};
    --icon-border: {{ section.settings.icon_border | default: '#2B1B12' }};
    --icon-text: {{ section.settings.icon_text_color | default: '#ffffff' }};
    --benefit-text: {{ section.settings.benefit_text_color | default: '#2B1B12' }};
    --content-width: {{ section.settings.content_width | default: 640 }}px;

    position: relative;
    overflow: hidden;
    background: radial-gradient(circle at 50% 20%, #CE774B 0%, var(--bg) 55%, #A8542F 100%);
    border-top: 1px solid var(--border-color);
    border-bottom: 1px solid var(--border-color);
    padding-top: {{ section.settings.padding_top | default: 64 }}px;
    padding-bottom: {{ section.settings.padding_bottom | default: 64 }}px;
    padding-left: 20px;
    padding-right: 20px;
    font-family: inherit;
    min-height: 85vh;
    display: grid;
    grid-template-columns: 240px 1fr 240px;
    align-items: center;
    justify-content: center;
    gap: 24px;
    box-sizing: border-box;
  }

  .maison-cta_section-{{ section.id }} .maison-cta_content {
    text-align: center;
    max-width: var(--content-width);
    margin: 0 auto;
    position: relative;
    z-index: 3;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 22px;
    width: 100%;
  }

  /* Badge */
  .maison-cta_section-{{ section.id }} .maison-cta_badge {
    background: var(--badge-bg);
    border-radius: 999px;
    padding: 8px 20px 8px 8px;
    display: inline-flex;
    align-items: center;
    gap: 10px;
    box-shadow: 0 6px 18px rgba(0,0,0,0.15);
  }

  .maison-cta_section-{{ section.id }} .maison-cta_avatars { display: flex; align-items: center; }

  .maison-cta_section-{{ section.id }} .maison-cta_avatar {
    width: 30px; height: 30px; border-radius: 50%;
    border: 2px solid var(--badge-bg);
    margin-left: -10px; object-fit: cover;
  }
  .maison-cta_section-{{ section.id }} .maison-cta_avatar:first-child { margin-left: 0; }

  .maison-cta_section-{{ section.id }} .maison-cta_avatar-placeholder {
    width: 30px; height: 30px; border-radius: 50%;
    background: #e2e8f0; display: flex; align-items: center; justify-content: center;
    border: 2px solid var(--badge-bg); font-size: 14px; margin-left: -10px;
  }

  .maison-cta_section-{{ section.id }} .maison-cta_badge-text b {
    font-size: 14px; color: var(--badge-text); font-weight: 800;
  }
  .maison-cta_section-{{ section.id }} .maison-cta_badge-text em {
    font-style: normal; color: var(--bg); font-weight: 600; font-size: 14px; margin-left: 4px;
  }

  /* Heading */
  .maison-cta_section-{{ section.id }} .maison-cta_heading {
    font-size: {{ section.settings.heading_font_size | default: 42 }}px;
    line-height: 1.15;
    color: var(--heading-color);
    font-weight: 800;
    letter-spacing: -0.5px;
    margin-bottom: {{ section.settings.heading_margin_bottom | default: 15 }}px;
  }

  .maison-cta_section-{{ section.id }} .maison-cta_highlight { color: var(--highlight-color); }
  .maison-cta_section-{{ section.id }} .maison-cta_heading .accent {
    color: var(--accent-color);
    display: inline-block;
  }

  .maison-cta_section-{{ section.id }} .maison-cta_description {
    max-width: 560px;
    color: var(--desc-color);
    font-size: 15.5px;
    line-height: 1.6;
    opacity: 0.95;
    margin: 0 auto;
  }

  /* Button */
  .maison-cta_section-{{ section.id }} .maison-cta_button {
    background: var(--btn-bg);
    color: var(--btn-text);
    border: 1px solid var(--btn-border);
    padding: 16px 34px;
    border-radius: 10px;
    font-weight: 700;
    font-size: 14px;
    letter-spacing: 0.5px;
    text-decoration: none;
    cursor: pointer;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    box-shadow: 0 10px 20px var(--btn-shadow);
    display: inline-block;
  }
  .maison-cta_section-{{ section.id }} .maison-cta_button:hover {
    transform: translateY(-3px);
    box-shadow: 0 14px 26px var(--btn-shadow);
  }

  /* Benefits Row */
  .maison-cta_section-{{ section.id }} .maison-cta_benefits {
    display: flex; gap: 26px; flex-wrap: wrap; justify-content: center; margin-top: 6px;
  }
  .maison-cta_section-{{ section.id }} .maison-cta_benefit {
    display: flex; align-items: center; gap: 6px; font-size: 13px; font-weight: 600; color: var(--benefit-text);
  }
  .maison-cta_section-{{ section.id }} .tick {
    width: 18px; height: 18px;
    border: 2px solid var(--icon-border);
    background-color: var(--icon-bg);
    color: var(--icon-text);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 11px; font-weight: 700; flex-shrink: 0;
  }

  /* ---------- SIDE SLIDING COLUMNS & FADE MASK ---------- */
  .maison-cta_section-{{ section.id }} .slide-col {
    position: relative;
    height: 100%;
    max-height: 650px;
    width: 100%;
    overflow: hidden;
    z-index: 1;
    pointer-events: none;
    -webkit-mask-image: linear-gradient(to bottom, transparent 0%, black 12%, black 88%, transparent 100%);
    mask-image: linear-gradient(to bottom, transparent 0%, black 12%, black 88%, transparent 100%);
  }

  .maison-cta_section-{{ section.id }} .slide-track {
    display: flex;
    flex-direction: column;
    gap: 20px;
    width: 100%;
  }

  .maison-cta_section-{{ section.id }} .slide-track.up {
    animation: maisonScrollUp {{ section.settings.marquee_speed | default: 22 }}s linear infinite;
  }

  .maison-cta_section-{{ section.id }} .slide-track.down {
    animation: maisonScrollDown {{ section.settings.marquee_speed | default: 22 }}s linear infinite;
  }

  @keyframes maisonScrollUp {
    from { transform: translateY(0); }
    to { transform: translateY(-50%); }
  }

  @keyframes maisonScrollDown {
    from { transform: translateY(-50%); }
    to { transform: translateY(0); }
  }

  .maison-cta_section-{{ section.id }} .cta-photo-card {
    background: {{ section.settings.card_bg | default: '#F1DABF' }};
    border-radius: 18px;
    padding: 14px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 10px;
    box-shadow: 0 10px 25px rgba(0,0,0,0.18);
    border: 1px solid {{ section.settings.card_border | default: 'rgba(255,255,255,0.25)' }};
    flex-shrink: 0;
    width: 100%;
  }

  .maison-cta_section-{{ section.id }} .cta-photo-card img {
    width: 100%;
    height: 160px;
    object-fit: cover;
    border-radius: 12px;
    display: block;
  }

  .maison-cta_section-{{ section.id }} .cta-photo-card span {
    font-size: 13px;
    font-weight: 600;
    color: var(--heading-color);
    text-align: center;
    letter-spacing: 0.2px;
  }

  /* HIDE LEGACY ROTATED CONTAINERS FULLY */
  .maison-cta_section-{{ section.id }} .maison-cta_image-left,
  .maison-cta_section-{{ section.id }} .maison-cta_image-right,
  .maison-cta_section-{{ section.id }} .maison-cta_image-mobile-group {
    display: none !important;
  }

  /* Respect reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .maison-cta_section-{{ section.id }} .slide-track.up,
    .maison-cta_section-{{ section.id }} .slide-track.down {
      animation: none !important;
    }
  }

  /* HIDE SLIDING IMAGE COLUMNS FULLY ON MOBILE */
  @media (max-width: 900px) {
    .maison-cta_section-{{ section.id }} {
      grid-template-columns: 1fr !important;
      padding-left: 15px;
      padding-right: 15px;
    }
    .maison-cta_section-{{ section.id }} .slide-col {
      display: none !important;
    }
    .maison-cta_section-{{ section.id }} .maison-cta_heading {
      font-size: 24px !important;
    }
    .maison-cta_section-{{ section.id }} .maison-cta_description {
      font-size: 14px;
      padding: 0 8px;
    }
  }
{% style %}

{% schema %}
{
  "name": "Custom CTA",
  "settings": [
    { "type": "header", "content": "Colors" },
    { "type": "color", "id": "background_color", "label": "Background Color", "default": "#C1663D" },
    { "type": "color", "id": "border_color", "label": "Border Color", "default": "rgba(255,255,255,0.2)" },
    { "type": "color", "id": "heading_color", "label": "Heading Main Color", "default": "#2B1B12" },
    { "type": "color", "id": "accent_text_color", "label": "Heading Accent Text Color", "default": "#FDF3E7" },
    { "type": "color", "id": "highlight_color", "label": "Heading Highlight Color", "default": "#2B1B12" },
    { "type": "color", "id": "description_color", "label": "Description Color", "default": "#3d2a1c" },
    { "type": "color", "id": "badge_bg", "label": "Badge Background", "default": "#ffffff" },
    { "type": "color", "id": "badge_text_color", "label": "Badge Text", "default": "#2B1B12" },
    { "type": "color", "id": "button_bg", "label": "Button Background", "default": "#2B1B12" },
    { "type": "color", "id": "button_text_color", "label": "Button Text Color", "default": "#ffffff" },
    { "type": "color", "id": "button_border", "label": "Button Border Color", "default": "#2B1B12" },
    { "type": "color", "id": "button_shadow_color", "label": "Button Shadow Color", "default": "rgba(0,0,0,0.25)" },
    { "type": "color", "id": "card_bg", "label": "Sliding Card Background", "default": "#F1DABF" },
    { "type": "color", "id": "card_border", "label": "Sliding Card Border Color", "default": "rgba(255,255,255,0.25)" },
    { "type": "color", "id": "icon_bg", "label": "Checkmark Icon Background", "default": "#2B1B12" },
    { "type": "color", "id": "icon_border", "label": "Checkmark Icon Border", "default": "#2B1B12" },
    { "type": "color", "id": "icon_text_color", "label": "Checkmark Icon Color", "default": "#ffffff" },
    { "type": "color", "id": "benefit_text_color", "label": "Benefit Text Color", "default": "#2B1B12" },

    { "type": "header", "content": "Infinite Sliding Tracks" },
    { "type": "checkbox", "id": "enable_marquee", "label": "Enable Sliding Image Columns (Desktop only, auto-hidden on mobile)", "default": true },
    { "type": "range", "id": "marquee_speed", "label": "Sliding Speed (Seconds)", "min": 5, "max": 60, "step": 1, "default": 22 },

    { "type": "header", "content": "Typography & Spacing" },
    { "type": "range", "id": "heading_font_size", "label": "Heading Font Size (px)", "min": 20, "max": 60, "step": 1, "default": 42 },
    { "type": "range", "id": "heading_margin_bottom", "label": "Heading Bottom Space / Margin (px)", "min": 0, "max": 60, "step": 2, "default": 15 },
    { "type": "range", "id": "content_width", "label": "Text Content Max Width (px)", "min": 400, "max": 900, "step": 10, "default": 640 },
    { "type": "range", "id": "padding_top", "label": "Padding Top (px)", "min": 0, "max": 150, "step": 2, "default": 64 },
    { "type": "range", "id": "padding_bottom", "label": "Padding Bottom (px)", "min": 0, "max": 150, "step": 2, "default": 64 },

    { "type": "header", "content": "Trust Badge" },
    { "type": "checkbox", "id": "show_badge", "label": "Show Trust Badge", "default": true },
    { "type": "text", "id": "badge_count", "label": "Bold Count Text", "default": "5,000+" },
    { "type": "text", "id": "badge_text", "label": "Badge Label Text", "default": "Happy Customers" },

    { "type": "header", "content": "Heading Content" },
    { "type": "text", "id": "heading_highlight", "label": "First Heading Words", "default": "DISCOVER YOUR" },
    { "type": "text", "id": "heading_rest", "label": "Accent Heading Words", "default": "HEALTHIEST SKIN NATURALLY" },

    { "type": "header", "content": "Description & Button" },
    { "type": "textarea", "id": "description", "label": "Description", "default": "Experience the nourishing power of 100% grass-fed tallow skincare crafted to deeply hydrate, strengthen your skin barrier, and restore a naturally healthy glow. Simple ingredients. Real results. No unnecessary chemicals." },
    { "type": "text", "id": "button_text", "label": "Button Text", "default": "SHOP BEST SELLERS" },
    { "type": "url", "id": "button_link", "label": "Button Link" },

    { "type": "header", "content": "Benefits Row" },
    { "type": "text", "id": "benefit_1", "label": "Benefit 1", "default": "100% Guarantee" },
    { "type": "text", "id": "benefit_2", "label": "Benefit 2", "default": "Free Shipping (US Only)" },
    { "type": "text", "id": "benefit_3", "label": "Benefit 3", "default": "Easy Returns" }
  ],
  "blocks": [
    {
      "type": "avatar",
      "name": "Badge Avatar",
      "settings": [
        { "type": "image_picker", "id": "image", "label": "Avatar Image" }
      ]
    },
    {
      "type": "photo",
      "name": "Sliding Image Card",
      "settings": [
        { "type": "image_picker", "id": "image", "label": "Card Image" },
        { "type": "text", "id": "title", "label": "Image Title / Label (Optional)", "default": "" },
        { "type": "select", "id": "side", "label": "Column Position (Desktop)", "options": [
          { "value": "left", "label": "Left Column (Slides Up)" },
          { "value": "right", "label": "Right Column (Slides Down)" }
        ], "default": "left" }
      ]
    },
    {
      "type": "slide",
      "name": "Sliding Image Card (Slide)",
      "settings": [
        { "type": "image_picker", "id": "image", "label": "Card Image" },
        { "type": "text", "id": "title", "label": "Image Title / Label (Optional)", "default": "" },
        { "type": "select", "id": "side", "label": "Column Position (Desktop)", "options": [
          { "value": "left", "label": "Left Column (Slides Up)" },
          { "value": "right", "label": "Right Column (Slides Down)" }
        ], "default": "left" }
      ]
    }
  ],
  "presets": [
    {
      "name": "Custom CTA",
      "blocks": [
        { "type": "avatar" },
        { "type": "avatar" },
        { "type": "avatar" },
        { "type": "photo", "settings": { "side": "left", "title": "Whipped Tallow Balm" } },
        { "type": "photo", "settings": { "side": "left", "title": "Herbal Face Cream" } },
        { "type": "photo", "settings": { "side": "left", "title": "Honey Glow Serum" } },
        { "type": "photo", "settings": { "side": "right", "title": "Coconut Body Butter" } },
        { "type": "photo", "settings": { "side": "right", "title": "Grass-Fed Tallow" } },
        { "type": "photo", "settings": { "side": "right", "title": "Chamomile Soothe" } }
      ]
    }
  ]
}
{% endschema %}
