<!-- sections/custom-shipping-returns.liquid -->
{%- style -%}
  .custom-policy-page-{{ section.id }} {
    background-color: {{ section.settings.bg_color | default: '#FAF7EF' }};
    padding-top: {{ section.settings.padding_top | default: 50 }}px;
    padding-bottom: {{ section.settings.padding_bottom | default: 70 }}px;
    font-family: 'Poppins', sans-serif;
    color: #111111;
  }

  .cp-container {
    max-width: 960px;
    margin: 0 auto;
    padding: 0 20px;
    box-sizing: border-box;
  }

  .cp-header {
    text-align: center;
    margin-bottom: 40px;
  }

  .cp-tag {
    display: inline-block;
    font-size: 12px;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #e63946;
    margin-bottom: 8px;
  }

  .cp-title {
    font-size: clamp(30px, 4vw, 44px);
    font-weight: 900;
    text-transform: uppercase;
    color: #111111;
    margin: 0 0 12px 0;
    line-height: 1.1;
  }

  .cp-subtitle {
    font-size: 15px;
    color: #555555;
    margin: 0;
  }

  /* Grid Highlights */
  .cp-highlights-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 20px;
    margin-bottom: 40px;
  }

  .cp-highlight-card {
    background: #ffffff;
    border: 2px solid #111111;
    border-radius: 14px;
    padding: 24px;
    box-shadow: 0 4px 0 0 #111111;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .cp-highlight-title {
    font-size: 16px;
    font-weight: 800;
    text-transform: uppercase;
    color: #111111;
    margin: 0;
  }

  .cp-highlight-desc {
    font-size: 13.5px;
    color: #555555;
    line-height: 1.5;
    margin: 0;
  }

  /* Main Body Card */
  .cp-body-card {
    background: #ffffff;
    border: 2px solid #111111;
    border-radius: 16px;
    padding: 40px;
    box-shadow: 0 6px 0 0 #111111;
    box-sizing: border-box;
  }

  .cp-section-block {
    margin-bottom: 32px;
  }

  .cp-section-block:last-child {
    margin-bottom: 0;
  }

  .cp-block-heading {
    font-size: 20px;
    font-weight: 800;
    text-transform: uppercase;
    color: #111111;
    margin: 0 0 14px 0;
  }

  .cp-block-text {
    font-size: 14.5px;
    line-height: 1.7;
    color: #444444;
  }

  .cp-block-text p {
    margin: 0 0 1em 0;
  }
  .cp-block-text p:last-child {
    margin: 0;
  }

  .cp-block-text ul {
    margin: 12px 0 16px 20px;
    padding: 0;
  }

  .cp-block-text li {
    font-size: 14px;
    line-height: 1.7;
    color: #444444;
    margin-bottom: 6px;
  }

  @media (max-width: 768px) {
    .cp-body-card {
      padding: 24px;
    }
  }
{%- endstyle -%}

<div class="custom-policy-page-{{ section.id }}">
  <div class="cp-container">
    
    <div class="cp-header">
      {%- if section.settings.tag_text != blank -%}
        <span class="cp-tag">{{ section.settings.tag_text }}</span>
      {%- endif -%}
      
      {%- if section.settings.title != blank -%}
        <h1 class="cp-title">{{ section.settings.title }}</h1>
      {%- endif -%}
      
      {%- if section.settings.subtitle != blank -%}
        <p class="cp-subtitle">{{ section.settings.subtitle }}</p>
      {%- endif -%}
    </div>

    <!-- Highlights Grid -->
    {%- assign highlight_blocks = section.blocks | where: "type", "highlight" -%}
    {%- if highlight_blocks.size > 0 -%}
      <div class="cp-highlights-grid">
        {%- for block in highlight_blocks -%}
          <div class="cp-highlight-card" {{ block.shopify_attributes }}>
            <h3 class="cp-highlight-title">{{ block.settings.title }}</h3>
            <p class="cp-highlight-desc">{{ block.settings.description }}</p>
          </div>
        {%- endfor -%}
      </div>
    {%- endif -%}

    <!-- Main Content Card -->
    <div class="cp-body-card">
      {%- assign policy_blocks = section.blocks | where: "type", "policy_section" -%}
      {%- if policy_blocks.size > 0 -%}
        {%- for block in policy_blocks -%}
          <div class="cp-section-block" {{ block.shopify_attributes }}>
            {%- if block.settings.heading != blank -%}
              <h2 class="cp-block-heading">{{ block.settings.heading }}</h2>
            {%- endif -%}
            
            {%- if block.settings.content != blank -%}
              <div class="cp-block-text">
                {{ block.settings.content }}
              </div>
            {%- endif -%}
          </div>
        {%- endfor -%}
      {%- else -%}
        <div class="cp-section-block">
          <h2 class="cp-block-heading">Shipping & Return Details</h2>
          <div class="cp-block-text">
            <p>Please add policy section blocks in Theme Customizer.</p>
          </div>
        </div>
      {%- endif -%}
    </div>

  </div>
</div>

{% schema %}
{
  "name": "Shipping & Return Policy",
  "settings": [
    {
      "type": "text",
      "id": "tag_text",
      "label": "Sub-tag Text",
      "default": "Sankofa Tallow"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Main Title",
      "default": "Shipping & Return Policy"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Fast delivery, 30-day risk-free satisfaction guarantee for all natural skincare products."
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#FAF7EF"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Padding Top",
      "default": 50
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 70
    }
  ],
  "blocks": [
    {
      "type": "highlight",
      "name": "Highlight Box",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Highlight Title",
          "default": "Fast Processing"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Highlight Description",
          "default": "Orders dispatched within 1–2 business days. Tracked shipping delivered to your doorstep."
        }
      ]
    },
    {
      "type": "policy_section",
      "name": "Policy Section Block",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Section Heading",
          "default": "Shipping Information"
        },
        {
          "type": "richtext",
          "id": "content",
          "label": "Section Content",
          "default": "<p>Content goes here.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Shipping & Return Policy"
    }
  ]
}
{% endschema %}
