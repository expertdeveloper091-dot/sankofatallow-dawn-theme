{{ 'main-product.css' | asset_url | stylesheet_tag }}
{{ 'rb-product.css' | asset_url | stylesheet_tag }}

{%- liquid
  assign theme_st = settings
  assign section_st = section.settings
  assign section_width = section_st.section_width
  assign enable_sticky_content_on_desktop = section_st.enable_sticky_content_on_desktop
  assign enable_skeleton_loading = section_st.enable_skeleton_loading
  assign desktop_media_width = section_st.desktop_media_width
  assign desktop_layout = section_st.desktop_layout
  assign image_zoom = section_st.image_zoom
  assign user_variants_image_group = false
  assign enable_sticky_add_cart_on_desktop = section_st.enable_on_desktop
  assign enable_sticky_add_cart_on_mobile = section_st.enable_on_mobile
  assign enable_frequently_bought_together = section_st.enable_frequently_bought_together
  if settings.color_swatch_trigger != blank and settings.enable_color_swatches != false
    assign option_color_name = settings.color_swatch_trigger | split: ','
  else
    assign option_color_name = 'Color'
  endif
  assign color_scheme = section_st.color_scheme
  assign products_group = product.metafields.custom.product_grouped
  assign color = false
  for option in product.options_with_values
    for itemColor in option_color_name
      if option.name == itemColor
        assign color = true
      endif
    endfor
  endfor
  if section_st.reset_spacing
    assign reset_spacing = ' remove_spacing'
  endif
  assign wishlist = pages.wishlist
  assign action_when_click_added_wishlist = theme_st.action_when_click_added_wishlist
-%}
{%- capture style -%}
  --section-pt: {{ section_st.padding_top }}; --section-pb: {{ section_st.padding_bottom }};--speed: {{ section_st.speed }}s; --font-size: {{ section_st.font_size }};
{%- endcapture -%}

<div
  id="MainProduct-{{ section.id }}"
  class="section sec__featured-product product__item-js rb-product-item-js {% if section_st.padding_top < 30 %} pt-min{% endif %}{% if section_st.padding_bottom < 30 %} pb-min{% endif %} color-{{ color_scheme }} gradient{{ reset_spacing }}"
  data-section="{{ section.id }}"
  data-color-trigger="{{ settings.color_swatch_trigger }}"
  data-desktop-layout="{{ desktop_layout }}"
  data-mobile-layout="{{ section_st.mobile_options }}"
  data-zoom="{{ image_zoom }}"
  style="{{ style | strip | strip_newlines }}"
>
  {{ 'component-deferred-media.css' | asset_url | stylesheet_tag }}
  {%- assign first_3_d_model = product.media | where: 'media_type', 'model' | first -%}
  {%- if first_3_d_model -%}
    {{ 'component-product-model.css' | asset_url | stylesheet_tag }}
    <link
      id="ModelViewerStyle"
      rel="stylesheet"
      href="https://cdn.shopify.com/shopifycloud/model-viewer-ui/assets/v1.0/model-viewer-ui.css"
      media="print"
      onload="this.media='all'"
    >
    <link
      id="ModelViewerOverride"
      rel="stylesheet"
      href="{{ 'component-model-viewer-ui.css' | asset_url }}"
      media="print"
      onload="this.media='all'"
    >
  {% endif %}
  {%- if image_zoom == 'external_zoom' or image_zoom == 'inner_zoom_circle' or image_zoom == 'inner_zoom_square' -%}
    {{ 'photoswipe.css' | asset_url | stylesheet_tag }}
    <script src="{{ 'drift.js' | asset_url }}" defer="defer"></script>
    {{ 'drift-basic.min.css' | asset_url | stylesheet_tag }}
  {% endif %}
  {% if image_zoom != 'no_zoom' %}
    {{ 'photoswipe.css' | asset_url | stylesheet_tag }}
  {% endif %}
  <script src="{{ 'photoswipe-init.js' | asset_url }}" defer="defer" type="module"></script>
  {% if enable_skeleton_loading == true %}
    {{ 'skeleton.css' | asset_url | stylesheet_tag }}
    <skeleton-page class="{{ section_width }} skeleton block">
  {% else %}
    <div class="{{ section_width }}">
  {% endif %}

  <div
    class="flex flex-cols gap flex-wrap main-product rb-main-product"
    style="--col-gap: 30px;--col-width: {{ desktop_media_width }}%"
  >
    {%- if product.media.size > 0 -%}
      <div class="product-detail__media rb-product-detail__media col-md-w-custom w-full">
        {%- liquid
          assign default_position = 0
          unless product.has_only_default_variant
            assign default_position = product.selected_or_first_available_variant.featured_image.position | minus: 1
          endunless
        -%}
        {% liquid
          assign position = 1
          for opt in product.options_with_values
            for color_trigger_name in option_color_name
              if opt.name == color_trigger_name
                assign position = opt.position
              endif
            endfor
          endfor
          assign default_color_option = ''
          if position == 1
            assign default_color_option = product.selected_or_first_available_variant.option1
          elsif position == 2
            assign default_color_option = product.selected_or_first_available_variant.option2
          elsif position == 3
            assign default_color_option = product.selected_or_first_available_variant.option3
          endif
          assign media = product.media
          if product.metafields.custom.use_variants_image_group != blank and product.metafields.custom.use_variants_image_group
            assign user_variants_image_group = true
            assign alt_tags = ''
            assign first = true
            for image in product.media
              if image.alt != blank
                assign exclude_image = false
                for option in product.options_with_values
                  for value in option.values
                    if image.alt == value
                      assign exclude_image = true
                    endif
                  endfor
                endfor
                if exclude_image == false
                  if first
                    assign alt_tags = image.alt
                    assign first = false
                  else
                    assign alt_tags = alt_tags | append: ', ' | append: image.alt
                  endif
                endif
              endif
            endfor
            assign variant_media = product.media | where: 'alt', default_color_option
            assign alt_tags_list = alt_tags | split: ', '
            assign other_media = ''
            assign added_alts = ''
            for image in product.media
              for alt_tag in alt_tags_list
                if image.alt == alt_tag
                  if added_alts contains alt_tag
                  else
                    assign other_media = other_media | append: image.alt | append: ','
                    assign added_alts = added_alts | append: alt_tag | append: ','
                  endif
                endif
              endfor
            endfor
            assign combined_media = variant_media
            assign other_media_array = other_media | split: ','
            for alt_tag in other_media_array
              assign media_to_add = product.media | where: 'alt', alt_tag
              assign combined_media = combined_media | concat: media_to_add
            endfor
            assign media = combined_media
          endif
          if media.size == 0
            assign media = product.media
          endif
        %}
        {% render 'media-gallery',
          images: media,
          section_st: section_st,
          theme_st: theme_st,
          product: product,
          section: section,
          position_index: default_position,
          desktop_layout: desktop_layout
        %}
        {% if wishlist == empty %}
          {% assign action_when_click_added_wishlist = 'remove' %}
        {% endif %}
        <button-wishlist
          tabindex="0"
          class="product-item__button pointer hidden-md product-item__button-wishlist tooltip relative btn-hover transition bg-white rounded-50 border w-50 h-50 inline-flex content-center skeleton-loading"
          data-product-id="{{ product.id }}"
          data-action="{{ action_when_click_added_wishlist }}"
          data-tooltip-remove="{{ 'products.product.actions.wishlist.remove' | t }}"
          data-tooltip-add="{{ 'products.product.actions.wishlist.add' | t }}"
          data-tooltip-redirect="{{ 'products.product.actions.wishlist.redirect' | t }}"
        >
          <svg width="14" height="13" fill="none">
            <use href="#icon-wishlist" />
          </svg>
          <span class="tooltip-content invisible rounded-3 absolute pointer-none">
            {{- 'products.product.actions.wishlist.add' | t -}}
          </span>
        </button-wishlist>
      </div>
    {% endif %}

    {% comment %} Product Information Snippet {% endcomment %}
    {% render 'rb-product-info', product: product, section: section %}

  </div>
  {% if enable_skeleton_loading == true %}
    </skeleton-page>
  {% else %}
    </div>
  {% endif %}
</div>

<script src="{{ 'main-product.js' | asset_url }}" defer="defer"></script>
<script src="{{ 'rb-product.js' | asset_url }}" defer="defer"></script>

{% schema %}
{
  "name": "Main Product (Clay)",
  "tag": "section",
  "class": "section main-product-section rb-main-product-section",
  "settings": [
    {
      "type": "select",
      "id": "section_width",
      "label": "Section Width",
      "default": "container",
      "options": [
        {
          "value": "container",
          "label": "Container"
        },
        {
          "value": "fluid_container",
          "label": "Fluid Container"
        },
        {
          "value": "stretch_width",
          "label": "Stretch Width"
        },
        {
          "value": "full_width",
          "label": "Full Width"
        }
      ]
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color Scheme",
      "default": "scheme-1"
    },
    {
      "type": "checkbox",
      "id": "enable_sticky_content_on_desktop",
      "default": true,
      "label": "Enable sticky content on desktop"
    },
    {
      "type": "checkbox",
      "id": "enable_skeleton_loading",
      "default": true,
      "label": "Enable skeleton loading"
    },
    {
      "type": "header",
      "content": "Media Settings"
    },
    {
      "type": "range",
      "id": "desktop_media_width",
      "min": 40,
      "max": 60,
      "step": 1,
      "unit": "%",
      "label": "Desktop Media Width",
      "default": 40
    },
    {
      "type": "select",
      "id": "desktop_layout",
      "options": [
        {
          "value": "thumbnail_left",
          "label": "Thumbnail Left"
        },
        {
          "value": "thumbnail_bottom",
          "label": "Thumbnail Bottom"
        },
        {
          "value": "hidden_thumbnail",
          "label": "Hidden Thumbnail"
        },
        {
          "value": "grid_1_column",
          "label": "Grid 1 Column"
        },
        {
          "value": "grid_2_column",
          "label": "Grid 2 Column"
        },
        {
          "value": "stack",
          "label": "Stack"
        }
      ],
      "default": "thumbnail_bottom",
      "label": "Desktop Gallery Layout"
    },
    {
      "type": "select",
      "id": "image_zoom",
      "options": [
        {
          "value": "no_zoom",
          "label": "No Zoom"
        },
        {
          "value": "open_lightbox",
          "label": "Open Lightbox"
        },
        {
          "value": "inner_zoom_circle",
          "label": "Inner Zoom (Circle)"
        },
        {
          "value": "inner_zoom_square",
          "label": "Inner Zoom (Square)"
        },
        {
          "value": "external_zoom",
          "label": "External Zoom"
        }
      ],
      "default": "no_zoom",
      "label": "Image Zoom Mode"
    },
    {
      "type": "checkbox",
      "id": "enable_video_looping",
      "default": true,
      "label": "Enable video looping"
    },
    {
      "type": "checkbox",
      "id": "enable_video_autoplay",
      "default": true,
      "label": "Enable video autoplay"
    },
    {
      "type": "header",
      "content": "Frequently Bought Together"
    },
    {
      "type": "checkbox",
      "id": "enable_frequently_bought_together",
      "default": true,
      "label": "Enable Frequently Bought Together"
    },
    {
      "type": "header",
      "content": "Sticky Add to Cart Bar"
    },
    {
      "type": "checkbox",
      "id": "enable_on_desktop",
      "default": true,
      "label": "Enable Sticky Add to Cart on desktop"
    },
    {
      "type": "checkbox",
      "id": "enable_on_mobile",
      "default": true,
      "label": "Enable Sticky Add to Cart on mobile"
    },
    {
      "type": "header",
      "content": "Mobile Options"
    },
    {
      "type": "select",
      "id": "mobile_options",
      "options": [
        {
          "value": "show_thumbnails",
          "label": "Show Thumbnails"
        },
        {
          "value": "hidden_thumbnail",
          "label": "Hide Thumbnails"
        }
      ],
      "default": "show_thumbnails",
      "label": "Mobile Layout Options"
    },
    {
      "type": "header",
      "content": "Section Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top Padding",
      "default": 0,
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom Padding",
      "default": 0,
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px"
    },
    {
      "type": "checkbox",
      "id": "reset_spacing",
      "label": "Remove Spacing",
      "default": false
    }
  ],
  "blocks": [
    {
      "type": "@app"
    },
    {
      "type": "title",
      "name": "Product Title",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_vendor",
          "label": "Show vendor name",
          "default": true
        }
      ]
    },
    {
      "type": "badges",
      "name": "Product Badges",
      "limit": 1
    },
    {
      "type": "review_and_sold",
      "name": "Review & Sold Products",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_review",
          "default": true,
          "label": "Show rating review"
        },
        {
          "type": "checkbox",
          "id": "show_sold_products",
          "default": true,
          "label": "Show fake sold count"
        },
        {
          "type": "textarea",
          "id": "sold",
          "label": "Sold quantity range"
        },
        {
          "type": "textarea",
          "id": "hours",
          "label": "Time interval hours"
        }
      ]
    },
    {
      "type": "price",
      "name": "Price",
      "limit": 1
    },
    {
      "type": "buy_buttons",
      "name": "Buy Buttons",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_dynamic_checkout",
          "default": true,
          "label": "Show dynamic checkout button"
        },
        {
          "type": "checkbox",
          "id": "show_gift_card_recipient",
          "default": true,
          "label": "Show gift card recipient form"
        },
        {
          "type": "checkbox",
          "id": "show_wishlist",
          "default": true,
          "label": "Show wishlist button"
        },
        {
          "type": "checkbox",
          "id": "show_compare",
          "default": true,
          "label": "Show compare button"
        }
      ]
    },
    {
      "type": "live_view",
      "name": "Live View Counter",
      "limit": 1,
      "settings": [
        {
          "type": "range",
          "id": "min_number",
          "label": "Minimum visitors",
          "default": 10,
          "min": 1,
          "max": 30,
          "step": 1
        },
        {
          "type": "range",
          "id": "max_number",
          "label": "Maximum visitors",
          "default": 40,
          "min": 31,
          "max": 50,
          "step": 1
        },
        {
          "type": "range",
          "id": "interval_time",
          "label": "Update interval (seconds)",
          "default": 10,
          "min": 1,
          "max": 60,
          "step": 1
        },
        {
          "type": "textarea",
          "id": "content",
          "label": "Counter text message",
          "default": "[count] peoples are viewing this right now"
        }
      ]
    },
    {
      "type": "variant_picker",
      "name": "Variant Picker",
      "limit": 1,
      "settings": [
        {
          "type": "select",
          "id": "variant_type",
          "label": "Picker type",
          "options": [
            {
              "value": "swatches",
              "label": "Color Swatches"
            },
            {
              "value": "dropdown",
              "label": "Dropdown"
            }
          ],
          "default": "swatches"
        },
        {
          "type": "checkbox",
          "id": "show_size_guide",
          "label": "Show size guide link",
          "default": false
        },
        {
          "type": "text",
          "id": "size_guide_label",
          "label": "Size guide label",
          "default": "Size guide"
        },
        {
          "type": "page",
          "id": "size_guide_page",
          "label": "Size guide page content"
        }
      ]
    },
    {
      "type": "delivery_return",
      "name": "Delivery & Return Box",
      "limit": 1,
      "settings": [
        {
          "type": "select",
          "id": "delivery_design",
          "label": "Design layout",
          "default": "horizontal",
          "options": [
            {
              "value": "vertical",
              "label": "Vertical list"
            },
            {
              "value": "horizontal",
              "label": "Horizontal columns"
            }
          ]
        },
        {
          "type": "select",
          "id": "delivery_content_icon",
          "label": "Delivery Icon",
          "default": "boat",
          "options": [
            {
              "value": "none",
              "label": "None"
            },
            {
              "value": "free_shipping",
              "label": "Free Shipping"
            },
            {
              "value": "check_badge",
              "label": "Check Badge"
            },
            {
              "value": "message_communications",
              "label": "Message"
            },
            {
              "value": "boat",
              "label": "Boat / Express"
            },
            {
              "value": "truck",
              "label": "Truck / Cargo"
            }
          ]
        },
        {
          "type": "richtext",
          "id": "delivery_content",
          "label": "Delivery Content",
          "default": "<p>Estimate delivery times: <strong>3-5 days International.</strong></p>"
        },
        {
          "type": "select",
          "id": "discount_content_icon",
          "label": "Discount Icon",
          "default": "discount",
          "options": [
            {
              "value": "none",
              "label": "None"
            },
            {
              "value": "discount",
              "label": "Discount Tag"
            }
          ]
        },
        {
          "type": "richtext",
          "id": "discount_content",
          "label": "Discount Content",
          "default": "<p>Use code <strong>'WELCOME15'</strong> for discount 15% on your first order.</p>"
        },
        {
          "type": "select",
          "id": "return_content_icon",
          "label": "Return Icon",
          "default": "free_shipping",
          "options": [
            {
              "value": "none",
              "label": "None"
            },
            {
              "value": "free_shipping",
              "label": "Free Shipping"
            },
            {
              "value": "check_badge",
              "label": "Check Badge"
            },
            {
              "value": "message_communications",
              "label": "Message"
            },
            {
              "value": "boat",
              "label": "Boat"
            },
            {
              "value": "truck",
              "label": "Truck"
            }
          ]
        },
        {
          "type": "richtext",
          "id": "return_content",
          "label": "Return Policy Content",
          "default": "<p>Free shipping and returns: <strong>On all orders over $150.</strong></p>"
        }
      ]
    },
    {
      "type": "trust_badge",
      "name": "Trust Badge",
      "limit": 1,
      "settings": [
        {
          "type": "inline_richtext",
          "id": "message",
          "label": "Trust message heading",
          "default": "Guarantee safe checkout"
        },
        {
          "type": "image_picker",
          "id": "trust_badge_image",
          "label": "Trust badge image"
        },
        {
          "type": "range",
          "id": "image_width",
          "label": "Image display width",
          "min": 80,
          "max": 400,
          "unit": "px",
          "default": 200,
          "step": 10
        }
      ]
    },
    {
      "type": "collapsible_row",
      "name": "Collapsible Row",
      "limit": 5,
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "default": "Collapsible row",
          "label": "Row Heading"
        },
        {
          "type": "richtext",
          "id": "content",
          "label": "Row Content"
        },
        {
          "type": "checkbox",
          "id": "use_product_description",
          "default": false,
          "label": "Use full product description as content"
        },
        {
          "type": "checkbox",
          "id": "open",
          "default": false,
          "label": "Open accordion by default"
        }
      ]
    },
    {
      "type": "countdown_timer",
      "name": "Countdown Timer",
      "limit": 1,
      "settings": [
        {
          "type": "select",
          "id": "timer_style",
          "options": [
            {
              "value": "default",
              "label": "Default"
            },
            {
              "value": "highlight",
              "label": "Highlight box"
            }
          ],
          "default": "default",
          "label": "Timer Visual Style"
        },
        {
          "type": "inline_richtext",
          "id": "content",
          "label": "Header Message",
          "default": "🔥 Hurry Up! Sale ends in:"
        }
      ]
    },
    {
      "type": "product_addons",
      "name": "Product Addons (Share, Question, Color Compare)",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_compare_color",
          "default": true,
          "label": "Show Compare Colors option"
        },
        {
          "type": "checkbox",
          "id": "show_ask_question",
          "default": true,
          "label": "Show Ask a Question form popup"
        },
        {
          "type": "checkbox",
          "id": "show_share",
          "default": true,
          "label": "Show Share social buttons"
        }
      ]
    },
    {
      "type": "short_description",
      "name": "Short Description",
      "limit": 1,
      "settings": [
        {
          "type": "number",
          "id": "excerpt_length",
          "label": "Word excerpt length limit",
          "default": 150
        }
      ]
    },
    {
      "type": "model_size",
      "name": "Model Size Measurements",
      "limit": 1,
      "settings": []
    },
    {
      "type": "pickup_avaiability",
      "name": "Pickup Availability",
      "limit": 1
    },
    {
      "type": "complementary_products",
      "name": "Complementary Products",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "default": "Pairs well with",
          "label": "Heading text"
        },
        {
          "type": "range",
          "id": "product_list_limit",
          "min": 1,
          "max": 10,
          "step": 1,
          "default": 10,
          "label": "Maximum products limit"
        },
        {
          "type": "range",
          "id": "products_per_page",
          "min": 1,
          "max": 4,
          "step": 1,
          "default": 3,
          "label": "Products per view slide"
        },
        {
          "type": "checkbox",
          "id": "show_navigation",
          "default": false,
          "label": "Show slider arrows"
        },
        {
          "type": "checkbox",
          "id": "show_pagination",
          "default": false,
          "label": "Show slider dots"
        },
        {
          "type": "checkbox",
          "id": "enable_quick_add",
          "label": "Enable quick add button",
          "default": false
        }
      ]
    },
    {
      "type": "product_custom_field",
      "name": "Product Personalization Field",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Section Heading",
          "default": "Add your personalization:"
        },
        {
          "type": "inline_richtext",
          "id": "description",
          "label": "Description text",
          "default": "Add your name, note or upload your customized idea image to personalise your item. Custom items cannot be returned or exchanged."
        },
        {
          "type": "checkbox",
          "id": "enable_field_text",
          "label": "Enable custom text input",
          "default": true
        },
        {
          "type": "text",
          "id": "field_label",
          "label": "Text input label"
        },
        {
          "type": "checkbox",
          "id": "enable_field_image",
          "label": "Enable custom file upload",
          "default": true
        },
        {
          "type": "text",
          "id": "field_image_label",
          "label": "File upload label"
        }
      ]
    },
    {
      "type": "stock_countdown",
      "name": "Stock Countdown",
      "limit": 1,
      "settings": [
        {
          "type": "textarea",
          "id": "message",
          "default": "Hurry up! Only [count] left in stock",
          "label": "Low stock warning message"
        },
        {
          "type": "range",
          "id": "stock_items",
          "min": 1,
          "max": 100,
          "step": 1,
          "default": 30,
          "label": "Low stock threshold quantity"
        }
      ]
    },
    {
      "type": "product_grouped",
      "name": "Product Grouped Links",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Grouped section title"
        }
      ]
    },
    {
      "type": "more_colors",
      "name": "More Colors Variants",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "More colors title"
        },
        {
          "type": "range",
          "min": 40,
          "max": 100,
          "step": 5,
          "default": 40,
          "id": "image_width",
          "label": "Variant thumbnail image width"
        }
      ]
    },
    {
      "type": "custom_liquid",
      "limit": 1,
      "name": "Custom Liquid Code",
      "settings": [
        {
          "type": "liquid",
          "id": "custom_liquid",
          "label": "Custom Liquid"
        }
      ]
    },
    {
      "type": "text",
      "limit": 1,
      "name": "Rich Text",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading"
        },
        {
          "type": "richtext",
          "id": "content",
          "label": "Text Body"
        }
      ]
    },
    {
      "type": "product_meta",
      "name": "Product Meta (SKU, Tags, Stock)",
      "limit": 1,
      "settings": [
        {
          "type": "checkbox",
          "id": "show_barcode",
          "label": "Show Barcode",
          "default": false
        },
        {
          "type": "checkbox",
          "id": "show_sku",
          "label": "Show SKU",
          "default": true
        },
        {
          "type": "checkbox",
          "id": "show_available",
          "label": "Show Stock Status",
          "default": true
        },
        {
          "type": "checkbox",
          "id": "show_tags",
          "label": "Show Product Tags",
          "default": true
        },
        {
          "type": "checkbox",
          "id": "show_type",
          "label": "Show Product Type",
          "default": true
        },
        {
          "type": "checkbox",
          "id": "show_collections",
          "label": "Show Collections List",
          "default": true
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Main Product (Clay)"
    }
  ]
}
{% endschema %}
