<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,400&display=swap" rel="stylesheet">

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  .custom-stats {
    --text-color: {{ section.settings.text_color | default: '#2C2609' }};
    --bg-color: {{ section.settings.bg_color | default: '#FAF9F5' }};
    --accent-color: {{ section.settings.accent_color | default: '#e7a621' }};
    --card-bg: {{ section.settings.card_bg | default: '#FFFFFF' }};
    --card-border: {{ section.settings.card_border | default: section.settings.text_color }};
    --card-header-bg: {{ section.settings.card_header_bg | default: section.settings.accent_color }};
    --card-title-color: {{ section.settings.card_title_color | default: section.settings.text_color }};
    --stat-number-color: {{ section.settings.stat_number_color | default: section.settings.text_color }};
    --stat-circle-bg: {{ section.settings.stat_circle_bg | default: 'rgba(231, 166, 33, 0.22)' }};
    --heading-size: {{ section.settings.heading_font_size | default: 36 }}px;
    --subheading-size: {{ section.settings.subheading_font_size | default: 15 }}px;
    background-color: var(--bg-color);
    color: var(--text-color);
    font-family: 'Poppins', sans-serif !important;
    position: relative;
    width: 100%;
    box-sizing: border-box;
    overflow: hidden;
  }

  .custom-stats__container {
    max-width: {{ section.settings.container_width | default: section.settings.max_width | default: 1200 }}px;
    margin: 0 auto;
    padding: 0 2rem;
    box-sizing: border-box;
  }

  .custom-stats__grid {
    display: grid;
    grid-template-columns: 1.1fr 1fr;
    gap: {{ section.settings.column_gap | default: 60 }}px;
    align-items: center;
  }

  @media screen and (max-width: 1280px) {
    .custom-stats__grid {
      gap: 3.5rem;
    }
  }

  @media screen and (max-width: 989px) {
    .custom-stats__container {
      padding: 0 1.5rem;
    }
    .custom-stats__grid {
      grid-template-columns: 1fr;
      gap: 4rem;
    }
    .custom-stats__media-col {
      order: 1;
    }
    .custom-stats__text-col {
      order: 2;
      align-items: center;
      text-align: center;
    }
  }

  /* Media column styles */
  .custom-stats__media-col {
    position: relative;
    width: 100%;
    display: flex;
    justify-content: center;
  }

  .custom-stats__image {
    display: block;
    width: 100%;
    height: auto;
    object-fit: cover;
    border-radius: {{ section.settings.image_radius | default: 16 }}px;
  }

  /* Text column styles */
  .custom-stats__text-col {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    width: 100%;
  }

  .custom-stats__heading {
    font-family: 'Poppins', sans-serif !important;
    font-size: var(--heading-size);
    font-weight: 900;
    line-height: 1.1;
    letter-spacing: -0.02em;
    text-transform: uppercase;
    color: var(--text-color);
    margin: 0 0 {{ section.settings.heading_margin_bottom | default: 20 }}px 0;
  }

  .custom-stats__subheading {
    font-size: var(--subheading-size);
    line-height: 1.5;
    color: var(--text-color);
    margin-bottom: 2.5rem;
    opacity: 0.95;
  }

  .custom-stats__subheading p {
    margin: 0;
  }

  /* Stats Card styles */
  .custom-stats__card {
    background-color: var(--card-bg);
    color: var(--text-color);
    border-radius: 16px;
    border: 2px solid var(--card-border);
    box-shadow: 0px 4px 0px 0px var(--card-border);
    overflow: hidden;
    max-width: 600px;
    width: 100%;
  }

  .custom-stats__card-header {
    background-color: var(--card-header-bg);
    padding: 1.4rem 2rem;
    text-align: center;
    border-bottom: 2px solid var(--card-border);
  }

  .custom-stats__card-title {
    font-size: 1.4rem;
    font-weight: 800;
    letter-spacing: 0.05em;
    color: var(--card-title-color);
    margin: 0;
    text-transform: uppercase;
  }

  .custom-stats__card-body {
    padding: 2.5rem;
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }

  .custom-stats__row {
    display: flex;
    align-items: center;
    gap: 2rem;
    text-align: left;
  }

  .custom-stats__circle {
    background-color: var(--stat-circle-bg);
    min-width: 72px;
    width: 72px;
    height: 72px;
    border-radius: 50%;
    border: 2px solid var(--card-border);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    box-shadow: 0px 2px 0px 0px var(--card-border);
  }

  .custom-stats__percentage {
    font-size: 2.2rem;
    font-weight: 900;
    color: var(--stat-number-color);
  }

  .custom-stats__row-content {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .custom-stats__row-heading {
    font-size: 1.4rem;
    font-weight: 800;
    letter-spacing: 0.02em;
    color: var(--text-color);
    text-transform: uppercase;
  }

  .custom-stats__row-text {
    font-size: 1.35rem;
    line-height: 1.45;
    color: var(--text-color);
    margin: 0;
    opacity: 0.9;
  }
{%- endstyle -%}

<div class="custom-stats section-{{ section.id }}-padding">
  <div class="custom-stats__container">
    <div class="custom-stats__grid">
      
      <!-- Media Column -->
      <div class="custom-stats__media-col">
        <div class="custom-stats__image-wrapper">
          {%- if section.settings.image != blank -%}
            {{ section.settings.image | image_url: width: 1200 | image_tag: loading: 'lazy', class: 'custom-stats__image' }}
          {%- else -%}
            <img src="https://evilgoods.com/cdn/shop/files/Image_v2.png?v=1751627774&width=1500" class="custom-stats__image" alt="The Skincare Industry Is Lying" loading="lazy">
          {%- endif -%}
        </div>
      </div>

      <!-- Text Column -->
      <div class="custom-stats__text-col">
        <h2 class="custom-stats__heading">{{ section.settings.heading }}</h2>
        <div class="custom-stats__subheading rte">
          {{ section.settings.subheading }}
        </div>

        {%- assign has_stats = false -%}
        {%- for block in section.blocks -%}
          {%- if block.type == 'statistic' -%}
            {%- assign has_stats = true -%}
            {%- break -%}
          {%- endif -%}
        {%- endfor -%}

        {%- if has_stats -%}
          <div class="custom-stats__card">
            <div class="custom-stats__card-header">
              <p class="custom-stats__card-title">{{ section.settings.card_title }}</p>
            </div>
            <div class="custom-stats__card-body">
              {%- for block in section.blocks -%}
                {%- if block.type == 'statistic' -%}
                  <div class="custom-stats__row" {{ block.shopify_attributes }}>
                    <div class="custom-stats__circle">
                      <span class="custom-stats__percentage">{{ block.settings.percentage }}</span>
                    </div>
                    <div class="custom-stats__row-content">
                      <span class="custom-stats__row-heading">{{ block.settings.label }}</span>
                      <p class="custom-stats__row-text">{{ block.settings.description }}</p>
                    </div>
                  </div>
                {%- endif -%}
              {%- endfor -%}
            </div>
          </div>
        {%- endif -%}
      </div>

    </div>
  </div>
</div>

{% schema %}
{
  "name": "Industry Statistics",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Section Colors"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background color",
      "default": "#FAF9F5"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#2C2609"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent color",
      "default": "#e7a621"
    },
    {
      "type": "color",
      "id": "card_bg",
      "label": "Card background",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "card_border",
      "label": "Card border color",
      "default": "#2C2609"
    },
    {
      "type": "color",
      "id": "card_header_bg",
      "label": "Card Header Background",
      "default": "#e7a621"
    },
    {
      "type": "color",
      "id": "card_title_color",
      "label": "Card Header Title Color",
      "default": "#2C2609"
    },
    {
      "type": "color",
      "id": "stat_number_color",
      "label": "Stat Number Percentage Color",
      "default": "#2C2609"
    },
    {
      "type": "color",
      "id": "stat_circle_bg",
      "label": "Stat Circle Background",
      "default": "rgba(231, 166, 33, 0.22)"
    },
    {
      "type": "header",
      "content": "Typography & Sizing"
    },
    {
      "type": "range",
      "id": "container_width",
      "min": 900,
      "max": 1600,
      "step": 10,
      "unit": "px",
      "label": "Section Max Container Width",
      "default": 1200
    },
    {
      "type": "range",
      "id": "heading_font_size",
      "min": 20,
      "max": 60,
      "step": 1,
      "unit": "px",
      "label": "Heading Font Size",
      "default": 36
    },
    {
      "type": "range",
      "id": "heading_margin_bottom",
      "min": 0,
      "max": 60,
      "step": 2,
      "unit": "px",
      "label": "Heading Bottom Space / Margin",
      "default": 20
    },
    {
      "type": "range",
      "id": "subheading_font_size",
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Subheading Font Size",
      "default": 15
    },
    {
      "type": "range",
      "id": "column_gap",
      "min": 20,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Desktop Column Gap",
      "default": 60
    },
    {
      "type": "header",
      "content": "Media Content"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Side image"
    },
    {
      "type": "header",
      "content": "Text Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "THE SKINCARE INDUSTRY IS LYING TO YOU"
    },
    {
      "type": "richtext",
      "id": "subheading",
      "label": "Subheading",
      "default": "<p>Normal moisturizers and skin care products are loaded with toxic chemicals that don't just sit on your skin — they penetrate your bloodstream and disrupt your body from within. <strong>These aren't just ineffective — they're actively harmful.</strong></p>"
    },
    {
      "type": "text",
      "id": "card_title",
      "label": "Stats Card Title",
      "default": "SHOCKING INDUSTRY STATISTICS:"
    },
    {
      "type": "header",
      "content": "Section Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding top",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding bottom",
      "default": 60
    }
  ],
  "blocks": [
    {
      "type": "statistic",
      "name": "Statistic Row",
      "settings": [
        {
          "type": "text",
          "id": "percentage",
          "label": "Percentage",
          "default": "78%"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "default": "HORMONE-DISRUPTING FRAGRANCES"
        },
        {
          "type": "text",
          "id": "description",
          "label": "Description",
          "default": "78% of drugstore lotions contain fragrance blends that trigger inflammation, breakouts, and endocrine issues."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Industry Statistics",
      "blocks": [
        {
          "type": "statistic",
          "settings": {
            "percentage": "78%",
            "label": "HORMONE-DISRUPTING FRAGRANCES",
            "description": "78% of drugstore lotions contain fragrance blends that trigger inflammation, breakouts, and endocrine issues."
          }
        },
        {
          "type": "statistic",
          "settings": {
            "percentage": "85%",
            "label": "TESTOSTERONE-LOWERING CHEMICALS",
            "description": "85% of standard body lotions contain ingredients scientifically linked to hormone suppression and fertility problems."
          }
        },
        {
          "type": "statistic",
          "settings": {
            "percentage": "87%",
            "label": "PORE-CLOGGING PETROLEUM",
            "description": "87% of drugstore moisturizers use petroleum byproducts that create a plastic-like barrier, trapping dirt and preventing skin from breathing naturally."
          }
        }
      ]
    }
  ]
}
{% endschema %}
